@model IEnumerable<TalentVN.ApplicationCore.Entities.Notify>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Message)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Teacher)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Message)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Teacher.TeacherID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NotifyID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NotifyID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NotifyID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
